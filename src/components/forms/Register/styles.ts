import styled from "styled-components";
import { gapForm, widthColumnForm } from "../../../globals";

export const WrapperRegisterForm = styled.div`
  display: flex;
  flex-direction: column;
  gap: ${gapForm};
  width: ${widthColumnForm};
`